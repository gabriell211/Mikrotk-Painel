{
  "name": "mikrotk-painel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "HYDRA Network Control — painel web seguro para administração do MikroTik RouterOS.",
  "scripts": {
    "dev": "npx vercel dev",
    "start": "npx vercel dev",
    "check": "node --check src/js/app.js && node --check src/js/firewall-avancado.js && node --check src/js/hydra.js && node --check src/js/email-security.js && node --check api/login.js && node --check api/logout.js && node --check api/session.js && node --check api/routeros.js && node --check api/email-security.js && node --check api/backup.js && node --check api/_lib/seguranca.js && node --check api/_lib/routeros.js && node --check api/_lib/email-security.js",
    "test": "node --test tests/*.test.js",
    "validate": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
