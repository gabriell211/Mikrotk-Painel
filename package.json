{
  "name": "mikrotk-painel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Painel web em Vanilla JS para administração segura do MikroTik RouterOS.",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "check": "node --check src/js/app.js && node --check api/login.js && node --check api/logout.js && node --check api/session.js && node --check api/routeros.js && node --check api/backup.js && node --check api/_lib/seguranca.js && node --check api/_lib/routeros.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "vercel": "^46.0.0"
  }
}
